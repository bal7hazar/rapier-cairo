# RB — rigid-body API completion (AP work package "Rigid-body API completion", 123 items)

## 1. Read first
`AGENTS.md`; `docs/API_PARITY.md` (section "WP: Rigid-body API completion" and the `RigidBody`, `RigidBodyBuilder`,
`RigidBodyActivation`, `RigidBodyAdditionalMassProps`, … owner tables — the exact list you close); `scripts/api_parity.py`
(how an item is judged `ported`: same public name on the Cairo candidates, or a `RENAMES` entry); `docs/adr/0001-upstream-divergences.md`;
on `main`: `crates/rapier_dynamics2d/src/{rigid_body_set.cairo,rigid_body.cairo,rigid_body/*}` (the stored components:
position, velocity, forces, mass props, damping, locked axes, activation, dominance, ccd flags, user data) and
`crates/rapier2d/src/{world.cairo,pipeline/user_changes.cairo}` (change flags and wake-ups on edits).
Upstream (`UP=/home/claude/git/refs`): `$UP/rapier/src/dynamics/rigid_body.rs`, `rigid_body_components.rs`.

## 2. Scope (file allowlist)
`crates/rapier_dynamics2d/src/{rigid_body_set.cairo,rigid_body.cairo,rigid_body/**}`, their tests, `crates/rapier2d/src/world.cairo`
and `pipeline/user_changes.cairo` only where a setter must raise upstream's change flag / wake-up, `docs/API_PARITY.md`
(regenerated by `python3 scripts/api_parity.py`, never by hand; you may add `RENAMES` entries in `scripts/api_parity.py`
when an upstream name cannot be used in Cairo — say why), and the snapshots that move. Forbidden: solver, narrow/broad
phase, geometry, `Scarb.toml`/`lib.cairo` (re-exports → Escalations).

## 3. Expected result
Every item of the package `ported`, or `excluded` only through the existing closed rules, or left `missing` with a
one-line reason in REPORT.md (e.g. needs CCD, lot CC). Upstream semantics exactly: getters read the stored component;
setters mirror upstream's `wake_up` argument, change flags (`RigidBodyChanges`) and side effects (e.g.
`set_body_type` recomputes what upstream recomputes, `lock_rotations` updates the effective inverse inertia,
`set_additional_mass(_properties)` recomputes mass properties from colliders); builder methods for every upstream
builder option in scope. Additional mass properties are part of this lot (stored, included in the mass recompute,
upstream's semantics). Energy helpers (`kinetic_energy`, `gravitational_potential_energy`) and predictions
(`predict_position_using_velocity(_and_forces)`) as upstream. No behaviour change for existing scenes: every
golden and scene test bit-identical, P3 scenes ≤ +0.5 % gas.

## 4. Efficiency
Mechanical API work: `#[inline(always)]` trivial getters; measure only where a setter does real work (mass
recompute). ≤ 800 lines per file (split the trait impl into sibling files if needed); ≤ 4 fuzz per module.

## 5. Tests
Table-driven tests per group (getters, setters + change flags + wake-up, builder round-trip, additional mass,
locks); existing tests unchanged; `python3 scripts/api_parity.py --check` passes.

## 6. Definition of done
Foreground gate (tool timeout 3600000 ms; crate-scoped runs via the build shims); `python3 scripts/api_parity.py`
then `--check`; `gas.py check` then module-filtered snapshots; conventional commits + trailer; push; `gh pr create
--base main --title "<what ships>" --body-file …`; wait for the checks to be registered, then `gh pr checks --watch`
until green; never merge; `REPORT.md` (Summary · Items closed / left with reasons · API · Deviations · Gas · Requested
re-exports · Escalations · PR URL). Memory rules apply.

## 7. Work autonomously, do not ask questions, do not widen the scope.
