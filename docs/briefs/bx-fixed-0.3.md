# BX — move to `fixed` / `glam` 0.3.0 from scarbs.xyz (division now rounds to nearest)

## 1. Read first
`AGENTS.md`; `docs/PLAN.md` (D1, D12, "Paused / resumed" notes); the root `Scarb.toml` on your branch
(the orchestrator already switched `fixed` and `glam` from git rev `ce18a0b` to the registry
`"0.3.0"` and let scarb regenerate `Scarb.lock` — that commit is your base, do not touch it).
What changed in `fixed` 0.3.0 (glam.cairo tag `v0.3.0`; source readable read-only at
`/home/claude/projects/glam.cairo/packages/fixed/src`): `Fixed / Fixed`, `FixedTrait::recip` and
`from_ratio` now **round to nearest, ties to even** (before: truncate toward zero); `%` stays the
exact truncated remainder; products and the fused `wide` kernels still floor once per output; new
`wide::Acc` and `wide::RecipNearest` (prepared divisor, bit-identical to `/`); panic messages are
`'Fixed: overflow'`, `'Fixed: division by zero'`, …. Read the `fixed` entries of `/home/claude/projects/glam.cairo/CHANGELOG.md` (search "round", "division", "recip") for anything else that moved.

## 2. Scope (file allowlist)
Every file under `crates/**/src/**` and `crates/**/tests/**` **only** where a test expectation, a
tolerance, a panic message or an explicit reliance on truncating division must change; every
`gas/**/*.snap` (you regenerate them ALL with `python3 scripts/gas.py snapshot`, the one time an
executor is allowed to); `examples/ball_drop/Scarb.lock` (regenerated by scarb, never by hand).
Forbidden: any `Scarb.toml`, `scripts/**`, `.github/**`, `docs/**` except your REPORT, `tools/golden/**`
(the golden vectors come from Rust f64 and do not change). No refactor, no optimisation, no API
change: this PR must be reviewable as "the scalar moved, here is every consequence".

## 3. Expected work
1. Build, then run the full suite; list every failure.
2. For each failure decide, and say which in REPORT: (a) the expected value was computed with the old
   truncating division → update the expectation (show old/new raw values); (b) a golden tolerance is
   now exceeded → investigate: the port must still match the f64 reference within the README
   tolerance, round-to-nearest should only bring it closer; a regression is a finding (escalate with
   numbers, do not widen tolerances silently); (c) engine code relied on truncation (e.g. a
   `div_rem`-like invariant, a `/` used as floor) → minimal fix, flagged.
3. `fuzz_kernels_against_composed` in `crates/rapier_geometry2d/src/mass.cairo` (line ~520) failed once
   in CI (counterexample `(r, hx, hy, len, d) = (106, 38871, 12913, 119, 201)`: 30447 vs 30441 raw,
   tolerance `4 + raw/2^28` = 4 for small values) and the fixed seed does not seem to be honoured under
   `--workspace`: make the tolerance model sound for small magnitudes (derive it from the number of
   floors in the composed oracle) and add that counterexample as a table test.
4. Regenerate all snapshots; produce the delta summary with `python3 scripts/gas.py diff` (before =
   `origin/main` snapshots): per crate, min / median / max relative change, and the 10 largest moves.

## 4. Efficiency
Nothing to optimise. `RecipNearest` / `wide::Acc` are NOT to be adopted here (separate lots).

## 5. Tests
All existing tests pass (the two slope scenes stay `#[ignore]`d); the new mass counterexample test.

## 6. Definition of done
Foreground gate (`scarb fmt --workspace && scarb lint --workspace --deny-warnings && scarb build
--workspace && snforge test --workspace`); `python3 scripts/gas.py snapshot`; `python3 scripts/gas.py
check`; `cd examples/ball_drop && scarb build`; conventional commits + trailer; push; `gh pr create` per
template with the delta summary in the body; `gh pr checks --watch` until green (including the
`execute` job); never merge; `REPORT.md` (Summary · Failures and decisions (a/b/c) · Gas delta summary ·
Deviations · Escalations · PR URL). Memory rules apply: one scarb/snforge command at a time, foreground,
tool timeout 3600000 ms.

## 7. Work autonomously, do not ask questions, do not widen the scope.
