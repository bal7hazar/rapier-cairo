# GS — slope golden traces with correct cuboid feature ids, un-ignore the slope scenes

## 1. Read first
`AGENTS.md`; `docs/PLAN.md` (D11; SD and DM findings); `tools/golden/README.md` ("Feature ids (f64
upstream bug)": `Cuboid::vertex_feature_id` reads `to_bits() >> 31 / >> 30`, the f32 sign bits, which
in `parry2d-f64` are mantissa bits → every cuboid vertex id is 0, every face id `0b110000`, so upstream
f64 warm-starts BOTH regenerated points of a cuboid manifold with the same data — SD proved this is
why the slope traces are wrong from step 4; and SD's slope diagnostics section); `tools/golden/Cargo.toml`
(`rapier2d-f64 =0.35.3`, `parry2d-f64 =0.30.2`, `parry2d =0.30.2` f32 for ids); `tools/golden/src/{scenes.rs,main.rs}`;
on `main`: `crates/rapier2d/tests/golden_scenes.cairo`, `golden_scenes/{builder,slope_diagnostics}.cairo`
(P2 replays, SD's counterfactuals including the test-only emulation of the f64 id bug). The published
crate sources are in `~/.cargo/registry/src/*/parry2d-f64-0.30.2` (read-only).

## 2. Scope (file allowlist)
`tools/golden/**` (Cargo.toml `[patch.crates-io]`, a vendored and patched copy of `parry2d-f64 0.30.2`
under `tools/golden/vendor/parry2d-f64/` — keep only what the build needs, the fix is the `>> 63 / >> 62`
bit extraction in `Cuboid::vertex_feature_id` (and anything else the same TODO covers), documented in
`tools/golden/vendor/README.md` with the exact diff; `Cargo.lock` regenerated by cargo),
`tools/golden/vectors/scenes.json`, `crates/rapier_golden/src/generated/scenes.cairo` (generated),
`crates/rapier_golden/tests/sanity.cairo` if counts move, `crates/rapier2d/tests/golden_scenes.cairo` and
`golden_scenes/*.cairo`, `gas/rapier2d_integrationtest/golden_scenes.snap`,
`gas/rapier_golden_integrationtest/sanity.snap`. Forbidden: engine crates, CI (`golden` job runs `cargo
run --release --locked` in `tools/golden` and diffs — the vendored patch must work under that command
unchanged; if it cannot, escalate), `contact_manifolds` vectors (they already carry f32 ids; do not
regenerate them unless the patch changes them — then say so and include them).

## 3. Expected result
Scene traces generated by the f64 engine **with correct feature ids** (every scene regenerated: report
which of ball_drop / ball_bounce / box_slope_stick / box_slope_slide / box_stack3 / pendulum changed
and by how much). Then, in Cairo: remove the test-only f64-id emulation from `slope_diagnostics.cairo`
(it models a bug the references no longer have), un-`#[ignore]` both slope replays, and judge
`box_stack3` again with the strict per-sample tolerances (P2 had to fall back to rest invariants:
report whether the corrected ids make the strict comparison pass). Tolerances stay the README's.
Dependency on DM (midpoint lever arms, merged before you start or rebasing onto it): without it the
slopes cannot pass; if they still fail, report per quantity and step like P2 did and keep them
`#[ignore]`d with the reason.

## 4. Efficiency
Not a gas package. Keep the golden JSON growth minimal (no new scenes).

## 5. Tests
Regeneration twice is idempotent; the scene tests as above; ≤ 800 lines per file.

## 6. Definition of done
`cargo run --release --locked` twice in `tools/golden` (second run: zero diff); foreground Cairo gate;
snapshot filters `rapier2d_integrationtest::golden_scenes` (+ `rapier_golden_integrationtest::sanity`
if touched); `gas.py check`; conventional commits + trailer; push; `gh pr create` per template; `gh pr
checks --watch` until green; never merge; `REPORT.md` (Summary · Patch (diff, vendored size) · Scene
changes · Slope and stack results (max ulp per quantity) · Deviations · Escalations · PR URL). Memory
rules apply (one heavy build at a time, foreground, tool timeout 3600000 ms; `cargo build --release`
counts as heavy: wrap it in `flock $HOME/orchestrator/heavy-build.lock`).

## 7. Work autonomously, do not ask questions, do not widen the scope.
