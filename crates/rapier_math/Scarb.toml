[package]
name = "rapier_math"
description = "Physics-side math on top of glam.cairo: Rot2, Pose2, fused kernels and scalar helpers (the glamx layer of rapier.cairo)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fixed.workspace = true
glam.workspace = true

[dev-dependencies]
snforge_std.workspace = true
assert_macros.workspace = true
rapier_testing = { path = "../rapier_testing" }
rapier_golden = { path = "../rapier_golden" }

[tool]
fmt.workspace = true
scarb.workspace = true
snforge.workspace = true
