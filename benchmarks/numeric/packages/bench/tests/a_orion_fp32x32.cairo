// GENERATED by scripts/gen_a.py - do not edit
use orion::numbers::fixed_point::core::FixedTrait;
use orion::numbers::fixed_point::implementations::fp32x32::core::{FP32x32, FP32x32Impl};
use cubit::f64::math::trig;
use numbench::bb::{bb, sink};

fn a() -> FP32x32 { FixedTrait::<FP32x32, u64>::new(bb(15032385536), false) } // 3.5
fn b() -> FP32x32 { FixedTrait::<FP32x32, u64>::new(bb(5368709120), true) } // -1.25
fn ang() -> FP32x32 { FixedTrait::<FP32x32, u64>::new(bb(3006477107), false) } // 0.7 rad
fn ex() -> FP32x32 { FixedTrait::<FP32x32, u64>::new(bb(5368709120), false) }
fn sq() -> FP32x32 { FixedTrait::<FP32x32, u64>::new(bb(5302428325695), false) } // 1234.5678

#[test]
fn base_b1() { let x = a(); sink(x); }

#[test]
fn base_b2() { let x = a(); let y = b(); sink(x); let _ = y; }

#[test]
fn base_b2b() { let x = a(); let y = b(); let _ = x; let _ = y; sink(false); }

#[test]
fn base_bi() { let n: u64 = bb(7); sink(FixedTrait::<FP32x32, u64>::new(n, false)); }

#[test]
fn base_bo() { let x = a(); sink(x.mag); }

#[test]
fn b2_add_mixed_sign() { let x = a(); let y = b(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Add::add(x, y)); }

#[test]
fn b2_add_same_sign() { let x = a(); let y = ex(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Add::add(x, y)); }

#[test]
fn b2_sub() { let x = a(); let y = b(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Sub::sub(x, y)); }

#[test]
fn b2_mul() { let x = a(); let y = b(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Mul::mul(x, y)); }

#[test]
fn b2_div() { let x = a(); let y = b(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Div::div(x, y)); }

#[test]
fn b1_neg() { let x = a(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32Neg::neg(x)); }

#[test]
fn b1_abs() { let x = b(); sink(FP32x32Impl::abs(x)); }

#[test]
fn b2b_lt() { let x = a(); let y = b(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32PartialOrd::lt(x, y)); }

#[test]
fn b2b_lt_same_sign() { let x = a(); let y = ex(); sink(orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32PartialOrd::lt(x, y)); }

#[test]
fn b1_sqrt() { let x = sq(); sink(FP32x32Impl::sqrt(x)); }

#[test]
fn b1_sin() { let x = ang(); sink(trig::sin(x)); }

#[test]
fn b1_sin_fast_lut() { let x = ang(); sink(trig::sin_fast(x)); }

#[test]
fn b1_cos() { let x = ang(); sink(trig::cos(x)); }

#[test]
fn b1_cos_fast_lut() { let x = ang(); sink(trig::cos_fast(x)); }

#[test]
fn b1_atan() { let x = ang(); sink(trig::atan(x)); }

#[test]
fn b1_atan_fast_lut() { let x = ang(); sink(trig::atan_fast(x)); }

#[test]
fn b1_exp() { let x = ex(); sink(FP32x32Impl::exp(x)); }

#[test]
fn b1_floor() { let x = a(); sink(FP32x32Impl::floor(x)); }

#[test]
fn b1_round() { let x = a(); sink(FP32x32Impl::round(x)); }

#[test]
fn bi_from_int() { let n: u64 = bb(7); sink(FixedTrait::<FP32x32, u64>::new_unscaled(n, false)); }

#[test]
fn bo_to_int_u32() { let x = a(); let r: u32 = orion::numbers::fixed_point::implementations::fp32x32::core::FP32x32TryIntoU32::try_into(x).unwrap(); sink(r); }
