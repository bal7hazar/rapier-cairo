// GENERATED by scripts/gen_b.py - do not edit
use cubit::f64::{Fixed, FixedTrait};
use numbench::reprs::cubit_fx::CubitF64Fx;
use numbench::bb::{bb, sink};
use numbench::reprs::Fx;
use numbench::vecs::{Vec2, Vec3, Mat3, dot2, cross2, length2, normalize2, mat3_mul_vec3};

fn a() -> Fixed { FixedTrait::new(bb(15032385536), false) }
fn b() -> Fixed { FixedTrait::new(bb(5368709120), true) }
fn c() -> Fixed { FixedTrait::new(bb(5368709120), false) }
fn sq() -> Fixed { FixedTrait::new(bb(5302428325695), false) }
fn va() -> Vec2<Fixed> { Vec2 { x: FixedTrait::new(bb(15032385536), false), y: FixedTrait::new(bb(5368709120), true) } }
fn vb() -> Vec2<Fixed> { Vec2 { x: FixedTrait::new(bb(3221225472), false), y: FixedTrait::new(bb(9126805504), false) } }
fn v3() -> Vec3<Fixed> { Vec3 { x: FixedTrait::new(bb(15032385536), false), y: FixedTrait::new(bb(5368709120), true), z: FixedTrait::new(bb(8589934592), false) } }
fn m3() -> Mat3<Fixed> { Mat3 { m00: FixedTrait::new(bb(1546188227), false), m01: FixedTrait::new(bb(2061584302), false), m02: FixedTrait::new(bb(3435973837), true), m10: FixedTrait::new(bb(3435973837), true), m11: FixedTrait::new(bb(2576980378), false), m12: FixedTrait::new(bb(0), false), m20: FixedTrait::new(bb(2061584302), false), m21: FixedTrait::new(bb(2748779069), false), m22: FixedTrait::new(bb(2576980378), false) } }

#[test]
fn base_b1() { let x = a(); sink(x); }

#[test]
fn base_b2() { let x = a(); let y = b(); let _ = y; sink(x); }

#[test]
fn base_b2b() { let x = a(); let y = b(); let _ = x; let _ = y; sink(false); }

#[test]
fn base_v2() { let p = va(); let q = vb(); let _ = q; sink(p.x); }

#[test]
fn base_v1() { let p = va(); sink(p.x); }

#[test]
fn base_v1v() { let p = va(); sink(p); }

#[test]
fn base_m3() { let m = m3(); let v = v3(); let _ = m; sink(v); }

#[test]
fn b2_add_mixed_sign() { let x = a(); let y = b(); sink(x + y); }

#[test]
fn b2_add_same_sign() { let x = a(); let y = c(); sink(x + y); }

#[test]
fn b2_sub() { let x = a(); let y = b(); sink(x - y); }

#[test]
fn b2_mul() { let x = a(); let y = b(); sink(x * y); }

#[test]
fn b2_div() { let x = a(); let y = b(); sink(x / y); }

#[test]
fn b1_neg() { let x = a(); sink(-x); }

#[test]
fn b2b_lt_mixed_sign() { let x = a(); let y = b(); sink(x < y); }

#[test]
fn b2b_lt_same_sign() { let x = a(); let y = c(); sink(x < y); }

#[test]
fn b1_sqrt() { let x = sq(); sink(Fx::sqrt(x)); }

#[test]
fn v2_dot2() { let p = va(); let q = vb(); sink(dot2(p, q)); }

#[test]
fn v2_cross2() { let p = va(); let q = vb(); sink(cross2(p, q)); }

#[test]
fn v1_length2() { let p = va(); sink(length2(p)); }

#[test]
fn v1v_normalize2() { let p = va(); sink(normalize2(p)); }

#[test]
fn m3_mat3_mul_vec3() { let m = m3(); let v = v3(); sink(mat3_mul_vec3(m, v)); }

#[test]
fn check_correctness() {
    { let e = (a() + b()); assert!(FixedTrait::new((9663672121), false) < e); assert!(e < FixedTrait::new((9663680711), false)); }
    { let e = (a() - b()); assert!(FixedTrait::new((20401090361), false) < e); assert!(e < FixedTrait::new((20401098951), false)); }
    { let e = (a() * b()); assert!(FixedTrait::new((18790486215), true) < e); assert!(e < FixedTrait::new((18790477625), true)); }
    { let e = (a() / b()); assert!(FixedTrait::new((12025912724), true) < e); assert!(e < FixedTrait::new((12025904134), true)); }
    { let e = (-a()); assert!(FixedTrait::new((15032389831), true) < e); assert!(e < FixedTrait::new((15032381241), true)); }
    assert!(b() < a()); assert!(!(a() < b())); assert!(c() < a());
    { let e = Fx::sqrt(sq()); assert!(FixedTrait::new((150909332441), false) < e); assert!(e < FixedTrait::new((150910191435), false)); }
    { let e = dot2(va(), vb()); assert!(FixedTrait::new((134222023), true) < e); assert!(e < FixedTrait::new((134213433), true)); }
    { let e = cross2(va(), vb()); assert!(FixedTrait::new((35970346809), false) < e); assert!(e < FixedTrait::new((35970355399), false)); }
    { let e = length2(va()); assert!(FixedTrait::new((15961890276), false) < e); assert!(e < FixedTrait::new((15962749269), false)); }
    let n = normalize2(va()); { let e = n.x; assert!(FixedTrait::new((4044321215), false) < e); assert!(e < FixedTrait::new((4045180208), false)); } { let e = n.y; assert!(FixedTrait::new((1444983322), true) < e); assert!(e < FixedTrait::new((1444124329), true)); }
    let r = mat3_mul_vec3(m3(), v3()); { let e = r.x; assert!(FixedTrait::new((4037273553), true) < e); assert!(e < FixedTrait::new((4037264963), true)); } { let e = r.y; assert!(FixedTrait::new((15247138196), true) < e); assert!(e < FixedTrait::new((15247129606), true)); } { let e = r.z; assert!(FixedTrait::new((8933527681), false) < e); assert!(e < FixedTrait::new((8933536271), false)); }
}
