// GENERATED by scripts/gen_a.py - do not edit
use orion::numbers::fixed_point::core::FixedTrait;
use orion::numbers::fixed_point::implementations::fp16x16::core::{FP16x16, FP16x16Impl, FP16x16Add, FP16x16Sub, FP16x16Mul, FP16x16Div, FP16x16PartialOrd, FP16x16Neg, FP16x16TryIntoU32};
use orion::numbers::fixed_point::implementations::fp16x16::math::trig;
use numbench::bb::{bb, sink};

fn a() -> FP16x16 { FixedTrait::<FP16x16, u32>::new(bb(229376), false) } // 3.5
fn b() -> FP16x16 { FixedTrait::<FP16x16, u32>::new(bb(81920), true) } // -1.25
fn ang() -> FP16x16 { FixedTrait::<FP16x16, u32>::new(bb(45875), false) } // 0.7 rad
fn ex() -> FP16x16 { FixedTrait::<FP16x16, u32>::new(bb(81920), false) }
fn sq() -> FP16x16 { FixedTrait::<FP16x16, u32>::new(bb(80908635), false) } // 1234.5678

#[test]
fn base_b1() { let x = a(); sink(x); }

#[test]
fn base_b2() { let x = a(); let y = b(); sink(x); let _ = y; }

#[test]
fn base_b2b() { let x = a(); let y = b(); let _ = x; let _ = y; sink(false); }

#[test]
fn base_bi() { let n: u32 = bb(7); sink(FixedTrait::<FP16x16, u32>::new(n, false)); }

#[test]
fn base_bo() { let x = a(); sink(x.mag); }

#[test]
fn b2_add_mixed_sign() { let x = a(); let y = b(); sink(x + y); }

#[test]
fn b2_add_same_sign() { let x = a(); let y = ex(); sink(x + y); }

#[test]
fn b2_sub() { let x = a(); let y = b(); sink(x - y); }

#[test]
fn b2_mul() { let x = a(); let y = b(); sink(x * y); }

#[test]
fn b2_div() { let x = a(); let y = b(); sink(x / y); }

#[test]
fn b1_neg() { let x = a(); sink(-x); }

#[test]
fn b1_abs() { let x = b(); sink(x.abs()); }

#[test]
fn b2b_lt() { let x = a(); let y = b(); sink(x < y); }

#[test]
fn b2b_lt_same_sign() { let x = a(); let y = ex(); sink(x < y); }

#[test]
fn b1_sqrt() { let x = sq(); sink(x.sqrt()); }

#[test]
fn b1_sin() { let x = ang(); sink(trig::sin(x)); }

#[test]
fn b1_sin_fast_lut() { let x = ang(); sink(trig::sin_fast(x)); }

#[test]
fn b1_cos() { let x = ang(); sink(trig::cos(x)); }

#[test]
fn b1_cos_fast_lut() { let x = ang(); sink(trig::cos_fast(x)); }

#[test]
fn b1_atan() { let x = ang(); sink(trig::atan(x)); }

#[test]
fn b1_atan_fast_lut() { let x = ang(); sink(trig::atan_fast(x)); }

#[test]
fn b1_exp() { let x = ex(); sink(x.exp()); }

#[test]
fn b1_floor() { let x = a(); sink(x.floor()); }

#[test]
fn b1_round() { let x = a(); sink(x.round()); }

#[test]
fn bi_from_int() { let n: u32 = bb(7); sink(FixedTrait::<FP16x16, u32>::new_unscaled(n, false)); }

#[test]
fn bo_to_int_u32() { let x = a(); let r: u32 = x.try_into().unwrap(); sink(r); }
