// GENERATED by scripts/gen_b.py - do not edit
use numbench::reprs::sm64::Sm64;
use numbench::bb::{bb, sink};
use numbench::reprs::Fx;
use numbench::vecs::{Vec2, Vec3, Mat3, dot2, cross2, length2, normalize2, mat3_mul_vec3};

fn a() -> Sm64 { Sm64 { mag: bb(15032385536), sign: false } }
fn b() -> Sm64 { Sm64 { mag: bb(5368709120), sign: true } }
fn c() -> Sm64 { Sm64 { mag: bb(5368709120), sign: false } }
fn sq() -> Sm64 { Sm64 { mag: bb(5302428325695), sign: false } }
fn va() -> Vec2<Sm64> { Vec2 { x: Sm64 { mag: bb(15032385536), sign: false }, y: Sm64 { mag: bb(5368709120), sign: true } } }
fn vb() -> Vec2<Sm64> { Vec2 { x: Sm64 { mag: bb(3221225472), sign: false }, y: Sm64 { mag: bb(9126805504), sign: false } } }
fn v3() -> Vec3<Sm64> { Vec3 { x: Sm64 { mag: bb(15032385536), sign: false }, y: Sm64 { mag: bb(5368709120), sign: true }, z: Sm64 { mag: bb(8589934592), sign: false } } }
fn m3() -> Mat3<Sm64> { Mat3 { m00: Sm64 { mag: bb(1546188227), sign: false }, m01: Sm64 { mag: bb(2061584302), sign: false }, m02: Sm64 { mag: bb(3435973837), sign: true }, m10: Sm64 { mag: bb(3435973837), sign: true }, m11: Sm64 { mag: bb(2576980378), sign: false }, m12: Sm64 { mag: bb(0), sign: false }, m20: Sm64 { mag: bb(2061584302), sign: false }, m21: Sm64 { mag: bb(2748779069), sign: false }, m22: Sm64 { mag: bb(2576980378), sign: false } } }

#[test]
fn base_b1() { let x = a(); sink(x); }

#[test]
fn base_b2() { let x = a(); let y = b(); let _ = y; sink(x); }

#[test]
fn base_b2b() { let x = a(); let y = b(); let _ = x; let _ = y; sink(false); }

#[test]
fn base_v2() { let p = va(); let q = vb(); let _ = q; sink(p.x); }

#[test]
fn base_v1() { let p = va(); sink(p.x); }

#[test]
fn base_v1v() { let p = va(); sink(p); }

#[test]
fn base_m3() { let m = m3(); let v = v3(); let _ = m; sink(v); }

#[test]
fn b2_add_mixed_sign() { let x = a(); let y = b(); sink(x + y); }

#[test]
fn b2_add_same_sign() { let x = a(); let y = c(); sink(x + y); }

#[test]
fn b2_sub() { let x = a(); let y = b(); sink(x - y); }

#[test]
fn b2_mul() { let x = a(); let y = b(); sink(x * y); }

#[test]
fn b2_div() { let x = a(); let y = b(); sink(x / y); }

#[test]
fn b1_neg() { let x = a(); sink(-x); }

#[test]
fn b2b_lt_mixed_sign() { let x = a(); let y = b(); sink(x < y); }

#[test]
fn b2b_lt_same_sign() { let x = a(); let y = c(); sink(x < y); }

#[test]
fn b1_sqrt() { let x = sq(); sink(Fx::sqrt(x)); }

#[test]
fn v2_dot2() { let p = va(); let q = vb(); sink(dot2(p, q)); }

#[test]
fn v2_cross2() { let p = va(); let q = vb(); sink(cross2(p, q)); }

#[test]
fn v1_length2() { let p = va(); sink(length2(p)); }

#[test]
fn v1v_normalize2() { let p = va(); sink(normalize2(p)); }

#[test]
fn m3_mat3_mul_vec3() { let m = m3(); let v = v3(); sink(mat3_mul_vec3(m, v)); }

#[test]
fn check_correctness() {
    { let e = (a() + b()); assert!(Sm64 { mag: (9663672121), sign: false } < e); assert!(e < Sm64 { mag: (9663680711), sign: false }); }
    { let e = (a() - b()); assert!(Sm64 { mag: (20401090361), sign: false } < e); assert!(e < Sm64 { mag: (20401098951), sign: false }); }
    { let e = (a() * b()); assert!(Sm64 { mag: (18790486215), sign: true } < e); assert!(e < Sm64 { mag: (18790477625), sign: true }); }
    { let e = (a() / b()); assert!(Sm64 { mag: (12025912724), sign: true } < e); assert!(e < Sm64 { mag: (12025904134), sign: true }); }
    { let e = (-a()); assert!(Sm64 { mag: (15032389831), sign: true } < e); assert!(e < Sm64 { mag: (15032381241), sign: true }); }
    assert!(b() < a()); assert!(!(a() < b())); assert!(c() < a());
    { let e = Fx::sqrt(sq()); assert!(Sm64 { mag: (150909332441), sign: false } < e); assert!(e < Sm64 { mag: (150910191435), sign: false }); }
    { let e = dot2(va(), vb()); assert!(Sm64 { mag: (134222023), sign: true } < e); assert!(e < Sm64 { mag: (134213433), sign: true }); }
    { let e = cross2(va(), vb()); assert!(Sm64 { mag: (35970346809), sign: false } < e); assert!(e < Sm64 { mag: (35970355399), sign: false }); }
    { let e = length2(va()); assert!(Sm64 { mag: (15961890276), sign: false } < e); assert!(e < Sm64 { mag: (15962749269), sign: false }); }
    let n = normalize2(va()); { let e = n.x; assert!(Sm64 { mag: (4044321215), sign: false } < e); assert!(e < Sm64 { mag: (4045180208), sign: false }); } { let e = n.y; assert!(Sm64 { mag: (1444983322), sign: true } < e); assert!(e < Sm64 { mag: (1444124329), sign: true }); }
    let r = mat3_mul_vec3(m3(), v3()); { let e = r.x; assert!(Sm64 { mag: (4037273553), sign: true } < e); assert!(e < Sm64 { mag: (4037264963), sign: true }); } { let e = r.y; assert!(Sm64 { mag: (15247138196), sign: true } < e); assert!(e < Sm64 { mag: (15247129606), sign: true }); } { let e = r.z; assert!(Sm64 { mag: (8933527681), sign: false } < e); assert!(e < Sm64 { mag: (8933536271), sign: false }); }
}
