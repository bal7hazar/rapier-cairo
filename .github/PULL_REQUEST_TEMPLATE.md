## Summary

<!-- Work package id from docs/PLAN.md and what changed -->

## Checklist

- [ ] `test_*` cover behaviour and edge cases
- [ ] `gas_*` probes for every public function and every candidate implementation (inputs through `opaque`)
- [ ] Rejected candidates kept under `#[cfg(test)] mod alternatives`
- [ ] Docs on public items (rounding direction, value range, panics)
- [ ] `scarb fmt --check`, `scarb lint`, `snforge test --workspace` pass
- [ ] `.gas-snapshot` regenerated by the orchestrator; delta table below

## Gas delta

<!-- output of `python3 scripts/gas.py diff` -->
